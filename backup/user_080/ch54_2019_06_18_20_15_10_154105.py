def junta_nome_sobrenome(lista_nome, lista_sobrenome):
    lista_correta=[]
    for i in range(0,len(lista_nome)):
        nome_completo=("lista_nome[i]"+" "+"lista_sobrenome[i]")
        lista_correta.append(nome_completo)
    return lista_correta
        