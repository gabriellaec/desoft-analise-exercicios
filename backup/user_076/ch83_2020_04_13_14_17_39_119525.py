def medias_por_inicial(nome_nota):
    inicial_média = []
    for i in nome_nota:
        if i[0] not in inicial_média:
            inicial_média[i[0]] == nome_nota[i]
        elif i[0] in inicial_média:
            inicial_média[i[0]] += nome_nota[i]/n
            