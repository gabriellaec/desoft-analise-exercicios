import math
kg=float(input('Quantos kg?'))
m=float(input('Quantos metros?'))
s=float(input('Quantos segundos?'))
def calcula_trabalho(F,cos,m):
    F=((kg*m)/(s**2))
    cos=math.cos(math.radians(θ))
    τ=F*cos*m
    return τ