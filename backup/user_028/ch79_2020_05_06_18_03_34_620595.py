def monta_dicionario(x,y):
    dicionario = {}
    for i in x:
        chaves = x[i]
    for e in y:
        valores = y[i]
    for chave in dicionario.keys():
        dic = dicionario[chaves]
        for valor in dicionario.values():
            dic = valores
    return dicionario
