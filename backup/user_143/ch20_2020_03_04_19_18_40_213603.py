# Pergunta quantos km
qts_km= float(input('quantos km:'))
def P(qts_km):
    if qts_km <= 200:
        y=qts_km*0,5
        return y
    else:
        y=qts_km*0,45
        return y
    y = P
    print (y)
    