# Pergunta quantos km
qts_km= float(input('quantos km:'))
def P(qts_km):
    if qts_km <= 200:
        P=qts_km*0,5
        return P
    else:
        P=qts_km*0,45
        return P
    print (P)
    