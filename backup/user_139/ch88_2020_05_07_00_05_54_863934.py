class Retangulo:
    def calcula_perimetro(self): #calcula perimetro do quadrado
        p = (2 * self.dx) + (2 * self.dy)
        return p
    def calcula_area(self): #calcula area do quadrado
        a = self.dx * self. dy
        return a