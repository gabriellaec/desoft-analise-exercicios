def inverte_dicionario(h):
    dk=dict.keys(h)
    dv=dict.values(h)
    dkey=[]
    dvalue=[]
    
    for a in dk:
       	dvalue.append(a)
        
    for b in dv:
       	dkey.append(b)
    
    z={}
    dkey=dict.keys(z)
    dvalue=dict.values(z)
    
    return (z)