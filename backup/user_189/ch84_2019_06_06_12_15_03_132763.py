def inverte_dicionario(h):
    dict.keys(h)=dk
    dict.values(h)=dv
    dkey=[]
    dvalue=[]
    
    for a in dk:
       	dvalue.append(a)
        
    for b in dv:
       	dkey.append(b)
    
    z=dict
    dkey=dict.keys(z)
    dvalue=dict.values(z)
    
    return (z)