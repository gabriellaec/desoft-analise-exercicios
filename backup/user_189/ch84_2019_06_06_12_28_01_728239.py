def inverte_dicionario(h):
    dk=dict.keys()
    dv=dict.values()
    dkey=[]
    dvalue=[]
    
    for a in dk:
       	dvalue.append(a)
        
    for b in dv:
       	dkey.append(b)
    
    z=dict
    dkey=dict.keys()
    dvalue=dict.values()
    
    return (z)