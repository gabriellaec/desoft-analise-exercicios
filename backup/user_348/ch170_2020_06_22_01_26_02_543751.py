def apaga_repetidos(texto):
    saida = ''
    for i in range(len(texto)):
        letra = texto[i]
        if letra not in saida:
            saida = saida + letra
        else:
            letra == *
            saida = saida + letra
    return saida
    