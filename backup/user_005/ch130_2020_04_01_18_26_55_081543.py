def monta_mala (lista):
    lista = []
    soma = sum(lista)
    pesado = True 
    while pesado:
        if soma > 23:
            del(
        if soma < 23:
            pesado = False 
            return lista
