def verifica_preco(tl,dl,tc):
    for i in dl.keys():
        if i==dl.keys(i):
            return dl.values(i)
        for dl.values(i) in tc.keys():
            if dl.values(i) in tc.keys():
                return tc.keys(dl.values(i))
        
        
