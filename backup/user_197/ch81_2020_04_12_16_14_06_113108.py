def interseccao_valores(dic1,dic2):
    v=dic1.values
    v2=dic2.values
    lista=[]

    for v in dic1:
        lista.append(v)
    for v2 in dic2:
        lista.append(v2)
    return lista
        
     