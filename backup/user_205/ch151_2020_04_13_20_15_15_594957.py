def classifica_lista(lista):
    if len(lista)<2:
        return "nenhum"
    cresce = lista[0]
    descresce = lista[0]
    for numeros in range(len(lista)-1):
        if lista[numeros+1] > cresce:
            
    