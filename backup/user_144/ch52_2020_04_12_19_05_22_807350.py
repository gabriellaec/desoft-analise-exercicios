def calcula_total_da_nota(precos,qntd):
    i = 0
    while i < len(precos):
        while e < len(qntd):
            nota = precos[i] * qntd[e]
            e +=1
        i+=1
    return nota
        