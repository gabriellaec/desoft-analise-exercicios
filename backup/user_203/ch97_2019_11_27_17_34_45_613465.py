def total_do_semstre_por_bairro(dicionario):
    novodic ={}
    valores = list(dicionario.values()) 
	chaves = list(dicionario.keys())
    for i in range(valores):
        novodic[valores[i] += valores[i+1]]
	return novodic
        
   
        
   
