def verifica_progressao(lista):
    n=0
    if len(l) == 1:
        return AG
    elif:
        if (len(set(lista)) <= 1):
            return AG
    elif:
        dif = l[1] - l[0]
        for i in range(len(lista) - 1):
            if (l[i+1] - l[i] == diff):
                n+=1
        if n==len(lista):
            return PA
    elif:
        razao = lista[1]/lista[0]
        for i in range(len(lista)-1):
            if li[i]/(li[i-1])==ratio: 
                n+=1
        if n==len(lista):
            return PG
    else:
        return NA
           
    
        