def primeiras_ocorrencias(texto):
    indice = dict()
    for letra in texto:
        if not letra in indice:
            for i in range(len(texto))
            indice[letra] = texto[i]
    return indice