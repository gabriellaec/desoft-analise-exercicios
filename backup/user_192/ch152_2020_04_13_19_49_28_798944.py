def verifica_preco(titulo, nome_cor, cores_preco):
    nome_cor = dict()
    cores_preco = dict()
    for nome in nome_cor.keys():
        if nome == titulo:
            c = nome_cor[cor1]
    for cor2 in cores_preco.keys():
        if c == cor2:
            preco_final = cores_preco[preco]
    return preco_final
            