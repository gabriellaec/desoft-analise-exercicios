def estritamente_crescente (lista):
    
    lista2 = []
    
    lista2.append(lista[0])
    
    if (len(lista) > 0)
    	for i in lista:
        	maior = lista[0]
        
        	if (lista[i] > maior):
            	lista2.append(lista[i])
            	maior = lista[i]
            
    return(lista2)