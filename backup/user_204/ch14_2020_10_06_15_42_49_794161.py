import math
v = 0
ang = 0
h = 0
g = 9.8
eq3 = 0
def calcula_distancia_do_projetil():
    eq = (v ** 2 / 2 * g)
    eq2 = (1 + math.sqrt(eq3))
    eq3 = 1 + (2 * g * h / v ** 2 (math.sin(ang))**2 )
    eq4 = math.sin(2 * ang)
    return (eq * eq2) * eq4