import math 
def calcula_distancia_do_projetil(x, y0, g):
    a = (v**2)/(2*g)
    b = 1 + ((1+ c/d)*1/2)
    e = math.sin(2*θ)
    c = 2 * g * y0
    d = (v**2) * (((math.sin(θ))**2)
    distancia = a * b * e
    return distancia 
                  
                 
                  