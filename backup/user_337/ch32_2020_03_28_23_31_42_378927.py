lista = []
def lista_primos(n):
    while len(lista) <= n:
    if p == 2:
        lista.append(p)
    else:
        if p%2 != 0:
            impar = 3
            while impar < p:
                if P%impar != 0:
                    lista.append℗
                    impar = impar + 2

    