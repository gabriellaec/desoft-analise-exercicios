import math
def calcula_trabalho(F,theta,s):
    trabalho = F * math.degrees(math.cos(math.radians(theta)) * s
    return trabalho

teta2=math.degrees(math.asin(n1*(math.sin(math.radians(teta1)))/n2))