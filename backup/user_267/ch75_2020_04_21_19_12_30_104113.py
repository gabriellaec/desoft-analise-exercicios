def verifica_primos(num):
    dicio = dict()
    num[0] = dicio[0]
    i = 
    if dicio[0]
def primos_entre(a, b):
    soma = 0
    for n in range(a+1, b, -2):
        if p % n != 0:
            if p >= a and p <= b:
                soma += 1
    return soma