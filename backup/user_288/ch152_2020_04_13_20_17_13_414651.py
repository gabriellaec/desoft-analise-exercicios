def verifica_preco (nome, nome_por_cor, preco_cores):
    nome = input ('Qual o livro?')
    nome_por_cor = {}
    preco_cores = {}
    for e in nome_por_cor:
        nome = nome_por_cor[0]
    for e in nome_por_cor.values():
        cor = nome[0]
    for e in preco_cores:
        cor = preco_cores [0]
    for e in preco_cores:
        preco = cor[0]
    return preco
               