class Retangulo:
    def calcula_perimetro(self):
    	self.perimetro = perimetro
    def calcula_area(self):
        self.area = area
rect = Retangulo()
rect.perimetro(2 * ponto.x + 2 * ponto.y)
rect.area(ponto.x * ponto.y)