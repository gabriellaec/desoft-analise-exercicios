n=float(input('Digite os números que devem ser inseridos na lista'))
lista_soma=[]:
while n>0:
    lista_soma.append(n)
    n=float(input('Digite os números que devem ser inseridos na lista'))
	if n<0:
    	lista_soma.reverse
		print(lista_soma)
        