class Retangulo:
    def __init__(self, pie, psd):
        self.p1 = pie
        self.p2 = psd
        
    def calcula_perimetro(self):
        pie.x - psd.x = a
        pie.y - psd.y = b
        return (a+b)*2
    
    def calcula_area(self):
        pie.x - psd.x = a
        pie.y - psd.y = b
        return a*b