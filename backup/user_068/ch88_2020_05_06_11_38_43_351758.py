class Ponto:
    def __init__(self, x, y):
        self.x = x
        self.y = y
class Retangulo:
    def calcula(self):
        calcula_perimetro(self) = 2*self.x  + 2* self.y
        calcula_area(self) = self.x * self.y
        return calcula_perimetro(self), calcula_area(self)
        
        