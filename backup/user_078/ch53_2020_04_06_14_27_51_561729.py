def soma_impares(numeros):
    x=0
    y=0
    lista=[]
    for i in range(len(numeros)):
        z=numeros[i]
        if z%2!=0:
            lista.append(z)
    for x in range(len(numeros)):
        w=lista[b]
        y=y+w
    return y