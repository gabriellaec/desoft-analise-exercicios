def remove_vogais(string):
    palavra = '' 
    for letra in string:

        if letra != 'a' or letra != 'e' or letra != 'i' or letra != 'o' or letra != 'u':
            palavra + letra
        
    
    return palavra