class Retangulo():
    def calcula_perimetro(self):
        dy = ponto1.y - ponto2.y
        dx = ponto1.x - ponto2.x
        return 2*dx + 2*dy
    def calcula_area(self):
        dy = ponto1.y - ponto2.y
        dx = ponto1.x - ponto2.x
        return dx*dy