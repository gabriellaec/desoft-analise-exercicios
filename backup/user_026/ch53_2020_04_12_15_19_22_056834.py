def soma_impares(lista):
    lista_impar=[]
    soma=0
    while i<len(lista):
        
        if lista[i]%2!=0:
            for i in len(lista):
                soma+=lista[i]
        else:
            
    return soma