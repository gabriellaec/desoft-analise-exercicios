def calcula_total_da_nota(precos,quant):
    lista=[]
    precos[0]*len(quant)
    for i in range(len(quant)):
        lista[i]=precos[i]*quant[i]
    soma=0
    for soma in range (len(lista)):
        soma+=lista
    return soma