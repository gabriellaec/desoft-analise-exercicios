def verifica_preco(nome,dicionario_livros,tab_cores):
    for nomes,key in dicionario_livros.items():
        if nomes == nome:
            for key,values in tab_cores.items():
                if key == key:
                    return values