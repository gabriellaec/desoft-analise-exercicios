def verifica_preco(x, y, z):
    valor_da_cor=list(y.values())
    nomes=list(y.keys())
    nome_do_livro=list(z.keys())
    valor_do_livro=list(z.values())
    for i in range(len(nomes))
        if x==nomes[i]:
            valor_do_nome=valor_da_cor[i]
            break
    cor_do_livro=valor_da_cor[i]
    for i in range(len(valor_do_livro)):
        if valor_do_nome==nome_do_livro[i]:
            return valor_do_livro[i]