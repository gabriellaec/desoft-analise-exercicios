def interseccao_valores (dic1, dic2):
    lista = []
    for valor in dic1 .values():
        lista .append (valor)
    for valoor in dic2 .values ():
        lista.append (valoor)
    return lista
    