class Retangulo((self):
    def __init__(self,x,y):
        self.centrox= centro.x
        self.centroy=centro.y
        self.raio=raio
     def calcula_perimetro(self):
                ladoX= (self.centrox- self.x)
                ladoY= (self.centroy- self.y)
                perimetro= ladox*2+ ladoY*2
      return perimetro
      def calcula_area(self):
                ladoX= (self.centrox- self.x)
                ladoY= (self.centroy- self.y)
                area= ladoX*ladoy
      return area