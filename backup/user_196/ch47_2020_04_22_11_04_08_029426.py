maior = 0
menor = 0
def estritamente_crescente(a):
    for l in range (0,len(a)):
        if l == 0:
            maior = a[l]
            menor = a[l]
        else:
            pos = 0
            while pos < len(lista):
            if valor <= lista[pos]:
                lista.insert(pos,valor)
                break
            pos+=1
    return a
