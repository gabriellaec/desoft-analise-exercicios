def calcula_total_da_nota (preco, quantidade):
    preco = []
    quantidade = []
    len(preco) == len(quantidade)
    i = 0 
    while i < len(quantidade):
        preco_total = preco[i]*quantidade[i]
        i += 1
    return preco_total