def verifica_primos(lista):
    u= len(lista)
    dicName.update({key:value})
    a+=1
    while a in range(len(dic)):
        if a==2:
            return True
        if a%0:
            return False
        else:
            return True
        