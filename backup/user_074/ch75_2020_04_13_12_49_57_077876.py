def verifica_primos(lista):
    u= len(lista)
    dic={}
    a+=1
    while a in range(len(lista)):
        if a==2:
            return True
        if a%0:
            return False
        else:
            return True
        