def verifica_primos(lista, x):
    u= len(lista)
    dicName.update({key:value})
    a+=1
    x!=a
    x!=0
    while a in range(len(dic)):
        if a==2:
            return True
        if a/x==0:
            return False
        else:
            return True
        