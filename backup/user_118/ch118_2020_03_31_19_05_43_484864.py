import math
def reflexao_total_interna(n1,n2):
    teta1=math.asin(math.sin(math.radians(L)
    teta2=math.asin((n1*math.sin(math.radians(teta1)))/n2)
    if teta2>teta1:
        return True
    else:
        return False
           