'''A função deve criar uma lista e inserir valores nela.
Ao criar essa lista, a função deve somar os valores de dentro dessa lista.
Caso a soma dos valores exceda 23, deve-se imprimir uma lista com ampenas os valores que somados podem
no maxímo se igular a 23
'''

def monta_mala(x):
    b = sum(x)
    if b > 23:
        c = sum(x)<23
        return(c)
    print(x[1,2,3])