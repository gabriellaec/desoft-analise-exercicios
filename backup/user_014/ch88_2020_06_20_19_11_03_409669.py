class Retangulo:
    # Criando os atributos x e y
    def __init__ (self, x, y):
        self.x = altura
        self.y = largura
    def calcula_perimetro(self):
        return self.x * 2 + self.y * 2
    def calcula_aread(self):
        return self.x * self.y