#Com o while
def soma_valores (valores):
    s = 0
    i = 0
    while i < len(valores):
        s += valores[i]
        i += 1
    return s

#Com o for
def soma_valores (len(valores)):
    s = 0
    i = 0
    for i in range(len(valores)):
        s += valores[i]
    return s