def calcula_aumento(salario):
    for x in salario
        salario = float(salario)
        if salario <= 1250:
            salario_novo = salario*1,15
            return salario_novo
        else:
            salario_novo = salario*1,1
            return salario_novo