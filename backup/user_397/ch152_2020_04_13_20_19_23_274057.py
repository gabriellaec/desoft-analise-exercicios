def verifica_preco(livros,cores,titulo):
    livros=dict()
    cores=dict()
    titulo = input("titulo do livro: ")
    for titulo in livros:
        livros[titulo] = cor
    if cor in cores:
        cores[cor] = preco
        
    print (preco)

    