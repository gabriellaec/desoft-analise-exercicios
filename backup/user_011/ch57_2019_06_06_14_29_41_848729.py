def soma_impares(numeros):
    soma_impares = 0
    i = 0
    while i < len(numeros):
        if numeros[i]/2 != int:
            soma_impares = soma_impares + numeros[i]
    i += 1
    return soma_impares
        