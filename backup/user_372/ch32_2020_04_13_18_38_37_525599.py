import math
def lista_primos(n,x):
    lista_n=[]
    n=len(lista_n):
    i=0
    while i<len(lista_n):        
        if x==2:
            lista_n.append(x)
        for x in range(2,n):
            if x%2!=0:
                lista_n.append(x)
        return lista_n
                
       
        