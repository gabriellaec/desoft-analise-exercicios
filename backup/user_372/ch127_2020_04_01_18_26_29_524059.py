import math
def calcula_elongacao(A,ϕ0,ω,t):
    if (ϕ0+w*t)=90
        A=x
    A=(cos*(ϕ0+w*t)
    ϕ0rad=ϕ0*(math.pi/180)
    w=((ϕ0rad-ϕrad)/
    t_inicial=0
    delta_t=t-t_inicial  
    ϕrad=ϕ*(math.pi/180)
    w=(ϕrad-ϕ0rad)/delta_t)*(math.pi/180)
    x=A*cos(ϕ0+w*t)
    
 