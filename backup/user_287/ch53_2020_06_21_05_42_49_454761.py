def soma_impares(lista):
    lista = []
    for i in range(len(lista)):
        if lista[i] not % 2:
            return False
        else:
            soma = lista.append(lista[i])
        return soma
           
            
            