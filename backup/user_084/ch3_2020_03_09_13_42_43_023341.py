import math
def calcula_gaussiana(x,μ,σ):
	f=(1*math.e**(-0.5*((x-μ)/σ)**2))/(σ*(2*math.pi)**-1)
	x=float(input("valor de x: ")
    μ=float(input("valor de mi: ")
    σ=float(input("valor de sigma: ")    
    return f
 

 
