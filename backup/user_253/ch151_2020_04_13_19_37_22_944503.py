def classifica_lista (lista):
    i=0
    while i> len(lisata) and len(lista) > 2:
        if len(lista)<2 and lista[0+i] < lista[1+i]:
            i+=1
        return 'crescente'
        if len (lista)<2 and lista[1+i]< lista[0+i]:
            i+=1
        return 'decrescente'
        elif:
            return 'nenhum'
    if len(lista) < 2:
        return 'nenhum'
    
  
   
    
    
    